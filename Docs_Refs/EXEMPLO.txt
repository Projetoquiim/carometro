Cenário A: Rotas no Frontend (Sem usar Express)Use este se você quer apenas trocar de página no navegador (ex: / para /contato) sem precisar de um servidor processando os dados.

1. Estrutura de arquivos:textseu-projeto/
├── public/
│   ├── index.html
│   └── script.js
└── firebase.json
Use o código com cuidado.

2. Código do public/index.html:html<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Meu App Estático</title>
</head>
<body>
    <nav>
        <button onclick="navegar('/')">Home</button>
        <button onclick="navegar('/contato')">Contato</button>
    </nav>

    <!-- As páginas serão injetadas aqui dentro -->
    <div id="conteudo"></div>

    <script src="/script.js"></script>
</body>
</html>
Use o código com cuidado.

3. Código do public/script.js (O roteador do navegador):

javascript// Define o que aparece em cada rota
const rotas = {
    '/': '<h1>Página Inicial</h1><p>Bem-vindo ao meu site!</p>',
    '/contato': '<h1>Contato</h1><p>Fale conosco em contato@email.com</p>',
    '/404': '<h1>404</h1><p>Página não encontrada.</p>'
};

// Função para mudar a URL e o conteúdo sem recarregar a página
function navegar(url) {
    window.history.pushState({}, "", url);
    processarRota();
}

// Renderiza o conteúdo baseado na URL atual
function processarRota() {
    const caminho = window.location.pathname;
    const conteudoDiv = document.getElementById('conteudo');
    conteudoDiv.innerHTML = rotas[caminho] || rotas['/404'];
}

// Escuta os botões de voltar/avançar do navegador
window.onpopstate = processarRota;

// Processa a rota assim que a página carrega
processarRota();
Use o código com cuidado.4. Código do firebase.json (Obrigatório para não dar erro 404 ao atualizar a página):json{
  "hosting": {
    "public": "public",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
Use o código com cuidado.


*************************************************************************************************************
exemplo completo do uso do abacate pay passo a passo de como devo fazer para vender com exemplos inclusive de banco de dados dos produtos com firebase firestorePara integrar a AbacatePay AbacatePay ao Firebase Firestore, o fluxo exige três etapas: estruturar o banco de dados de produtos, gerar o link de pagamento via API e atualizar o status do pedido em tempo real usando Webhooks para concluir a venda.1. Modelagem do Banco de Dados (Firebase Firestore)No seu painel do Firebase Console, crie duas coleções principais: produtos e pedidos.Coleção: produtosCada documento contém os dados base que serão puxados na hora da compra.Documento ID: produto_123Campos:nome: "Curso Completo de Abacate"preco: 97.00 (Número)descricao: "Aprenda a faturar vendendo abacates"ativo: true (Boolean)Coleção: pedidosArmazena a intenção de compra antes do cliente pagar.Documento ID: pedido_abc789Campos:clienteId: "usuario_456"produtoId: "produto_123"valor: 97.00status: "PENDENTE"criadoEm: Timestamp2. Passo a Passo para Gerar a VendaNo seu frontend ou backend (Node.js/Next.js), quando o usuário clicar em comprar, você deve chamar a API da AbacatePay:Autenticação: Obtenha sua chave de API no Dashboard da AbacatePay.Criar a Cobrança: Envie uma requisição POST para a API da AbacatePay.Exemplo de requisição (Body):json{
  "price": 9700,
  "currency": "BRL",
  "method": "PIX",
  "customer": {
    "name": "João da Silva",
    "email": "joao@email.com",
    "taxId": "12345678909"
  },
  "referenceId": "pedido_abc789" 
}
Use o código com cuidado.A AbacatePay retornará uma URL de pagamento (ex: https://abacatepay.com) e um ID de transação. Redirecione o cliente para essa URL.3. Atualização via Webhook (Backend Firebase)Para saber se o cliente pagou e liberar o produto, configure a URL de Webhook no painel da AbacatePay apontando para uma Firebase Cloud Function. Quando o pagamento for aprovado, a AbacatePay enviará um POST:Código de exemplo da Cloud Function:javascriptconst functions = require("firebase-functions");
const admin = require("firebase-admin");
admin.initializeApp();

exports.abacateWebhook = functions.https.onRequest(async (req, res) => {
    const dadosPagamento = req.body;
    
    // Verifica se a transação foi aprovada
    if (dadosPagamento.status === "PAID") {
        const pedidoId = dadosPagamento.referenceId; // Pega o ID salvo no passo 2

        // Atualiza o Firestore
        const pedidoRef = admin.firestore().collection("pedidos").doc(pedidoId);
        await pedidoRef.update({
            status: "CONCLUIDO",
            atualizadoEm: admin.firestore.FieldValue.serverTimestamp()
        });
        
        return res.status(200).send("Webhook recebido e processado com sucesso");
    }
    
    return res.status(200).send("Status ignorado");
});
Use o código com cuidado.Se quiser seguir com essa implementação, posso te ajudar com:As regras de segurança para proteger seu banco de dados Firestore.O código frontend em React para criar a interface da loja.A criação e configuração do Firebase Project.Qual dessas opções você quer ver aprofundada agora?2 sitesEntenda Gateway de pagamentos em 5 min - AbacatepayVideo sem enrolação, Direto ao ponto, do jeito que a gente gosta. Acesse nosso site: www.abacatepay.com.br Neste vídeo, explicamos...3:14YouTube·🥑 AbacatePay - Gateway de PagamentosAbacatePay é a plataforma mais descomplicada quando o assunto é receber pagamentos! Nesse vídeo vamos nos aprofundar um pouco mais na plataforma, vou te contar um pouco de tudo que você precisa… | AbacatepayE    ORL de retorno.    l   dentro do checkout ele tem uma setinha de voltar que ele pode voltar para onde ele estava antes de cli...pt.linkedin.comVocê disse: No passo a passo para gerar a venda, pede-se para que eu crie uma requisição post para a API da AbacatePay, como devo fazer isso? Como seria o meu formulário o javascript para que seja feito o que foi pedido no passo a passo?Para fazer essa requisição com segurança, você precisa entender uma regra crucial: nunca faça requisições diretas para a API da AbacatePay usando o JavaScript do Frontend. Se você fizer isso, sua chave secreta de API ficará visível para qualquer pessoa no navegador, permitindo que roubem seus dados ou criem cobranças falsas.O fluxo correto e seguro é:O Frontend (Formulário) envia os dados do cliente para o seu Backend (uma API própria ou Firebase Function).O seu Backend recebe esses dados, cria o pedido no Firestore, faz o POST seguro para a AbacatePay e recebe o link de pagamento.O seu Backend devolve esse link para o Frontend, que redireciona o usuário.Abaixo está o exemplo prático completo utilizando HTML, JavaScript Vanilla (Frontend) e um servidor Node.js (Backend).1. O Formulário (HTML)Crie uma interface simples para o cliente preencher os dados de faturamento.html<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Finalizar Compra</title>
</head>
<body>
    <h2>Preencha seus dados para pagar com PIX</h2>
    <form id="form-pagamento">
        <!-- Input escondido com o ID do produto que criamos no Firestore -->
        <input type="hidden" id="produto-id" value="produto_123">

        <label>Nome Completo:</label><br>
        <input type="text" id="nome" required><br><br>

        <label>E-mail:</label><br>
        <input type="email" id="email" required><br><br>

        <label>CPF:</label><br>
        <input type="text" id="cpf" placeholder="Apenas números" required><br><br>

        <button type="submit" id="btn-comprar">Pagar Agora</button>
    </form>

    <script src="frontend.js"></script>
</body>
</html>
Use o código com cuidado.2. O Script de Envio (frontend.js)Este script escuta o envio do formulário, envia os dados para a sua rota do backend e redireciona o usuário para a AbacatePay após receber a resposta.javascriptdocument.getElementById('form-pagamento').addEventListener('submit', async (event) => {
    event.preventDefault(); // Evita a página de recarregar

    const btn = document.getElementById('btn-comprar');
    btn.innerText = "Processando...";
    btn.disabled = true;

    // Captura os dados digitados pelo usuário
    const dadosCompra = {
        produtoId: document.getElementById('produto-id').value,
        nome: document.getElementById('nome').value,
        email: document.getElementById('email').value,
        cpf: document.getElementById('cpf').value.replace(/\D/g, '') // Remove pontos e traços do CPF
    };

    try {
        // Envia os dados para o SEU servidor backend
        const resposta = await fetch('http://localhost:3000/api/criar-cobranca', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(dadosCompra)
        });

        const resultado = await resposta.json();

        if (resultado.urlPagamento) {
            // Redireciona o usuário para a tela oficial de pagamento da AbacatePay
            window.location.href = resultado.urlPagamento;
        } else {
            alert('Erro ao gerar pagamento: ' + resultado.error);
            btn.innerText = "Pagar Agora";
            btn.disabled = false;
        }
    } catch (error) {
        console.error('Erro na requisição:', error);
        alert('Erro de conexão com o servidor.');
        btn.innerText = "Pagar Agora";
        btn.disabled = false;
    }
});
Use o código com cuidado.3. O Processador Seguro (Backend em Node.js)Aqui está o código do seu servidor (pode ser Express.js ou adaptado para uma Firebase Cloud Function). Ele busca o preço real do produto no Firestore para o cliente não fraudar o valor, cria o pedido e faz o POST oficial para a AbacatePay.javascriptconst express = require('express');
const cors = require('cors');
const admin = require('firebase-admin');

// Inicialize o Firebase Admin
admin.initializeApp({ projectId: "seu-projeto-id" });
const db = admin.firestore();

const app = express();
app.use(express.json());
app.use(cors()); // Permite que o frontend acesse o backend

const ABACATE_API_KEY = "Sua_Chave_Secreta_Da_AbacatePay_Aqui";

app.post('/api/criar-cobranca', async (req, res) => {
    try {
        const { produtoId, nome, email, cpf } = req.body;

        // 1. Busca o produto direto no Firestore para validar o preço real
        const produtoDoc = await db.collection('produtos').doc(produtoId).get();
        if (!produtoDoc.exists) {
            return res.status(404).json({ error: 'Produto não encontrado' });
        }
        const produto = produtoDoc.data();

        // 2. Registra a intenção de compra na coleção "pedidos" com status PENDENTE
        const novoPedidoRef = db.collection('pedidos').doc(); // Cria um ID automático
        await novoPedidoRef.set({
            cliente: { nome, email, cpf },
            produtoId: produtoId,
            valor: produto.preco,
            status: "PENDENTE",
            criadoEm: admin.firestore.FieldValue.serverTimestamp()
        });

        // 3. Monta e envia o POST oficial para a AbacatePay usando a Fetch API nativa
        const respostaAbacate = await fetch('https://abacatepay.com', {
            method: 'POST',
            headers: {
                'Authorization': `Bearer ${ABACATE_API_KEY}`,
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                frequency: "ONE_TIME", // Cobrança única
                methods: ["PIX"],      // Apenas PIX
                products: [
                    {
                        externalId: produtoId,
                        name: produto.nome,
                        quantity: 1,
                        priceValue: Math.round(produto.preco * 100) // AbacatePay exige o valor em centavos (Ex: R$ 97,00 vira 9700)
                    }
                ],
                returnUrl: "https://sualoja.com", // Para onde o cliente volta após pagar
                completionUrl: "https://sualoja.com",
                customerId: cpf, // Identificador único do cliente
                customer: {
                    name: nome,
                    email: email,
                    taxId: cpf
                }
            })
        });

        const dadosAbacate = await respostaAbacate.json();

        if (!respostaAbacate.ok) {
            throw new Error(dadosAbacate.message || 'Erro na API da AbacatePay');
        }

        // 4. Salva o ID da cobrança da AbacatePay no pedido do Firestore para conciliação futura
        await novoPedidoRef.update({
            abacatePayId: dadosAbacate.data.id
        });

        // 5. Devolve a URL de pagamento para o frontend redirecionar o usuário
        res.json({ urlPagamento: dadosAbacate.data.url });

    } catch (error) {
        console.error('Erro no servidor:', error.message);
        res.status(500).json({ error: 'Falha interna ao processar a cobrança.' });
    }
});

app.listen(3000, () => console.log('Servidor rodando na porta 3000'));
Use o código com cuidado.Se você quiser avançar, posso te mostrar:Como rodar esse código de backend diretamente nas Firebase Cloud Functions (eliminando a necessidade de manter um servidor Node.js próprio).Como testar esse fluxo localmente em ambiente de Sandbox / Testes.Como estruturar a página de obrigado que valida se o pagamento foi concluído.Como gostaria de prosseguir?Você disse: Como rodar esse código de backend diretamente nas Firebase Cloud Functions (eliminando a necessidade de manter um servidor Node.js próprio).Para migrar o código do Express para as Firebase Cloud Functions (2ª Geração), você usará o framework do Firebase para expor uma rota HTTP segura. Isso elimina a necessidade de gerenciar um servidor próprio.Aqui está o passo a passo completo para configurar o ambiente e implantar o código.1. Configurando o Ambiente das Cloud FunctionsNo terminal do seu computador, instale a ferramenta do Firebase se ainda não tiver e inicie o projeto:bash# Instala a CLI do Firebase globalmente
npm install -g firebase-tools

# Faz login na sua conta Google vinculada ao Firebase
firebase login

# Inicia o assistente de configuração no diretório do seu projeto
firebase init functions
Use o código com cuidado.Durante o assistente firebase init:Selecione Use an existing project e escolha o seu projeto do Firestore.Selecione JavaScript (ou TypeScript, se preferir).Responda Yes para instalar as dependências com o npm.Isso criará uma pasta chamada functions no seu projeto.2. Instalando as Dependências InternasNavegue até a pasta functions que foi criada e instale o pacote cors (necessário para o seu formulário frontend conseguir se conectar à função):bashcd functions
npm install cors
Use o código com cuidado.(Nota: O firebase-admin e o firebase-functions já vêm instalados por padrão nesta pasta).3. O Código da Cloud Function (functions/index.js)Substitua o conteúdo do arquivo functions/index.js pelo código abaixo. Ele realiza exatamente o mesmo papel do servidor Node.js anterior, mas rodando de forma 100% serverless:javascriptconst { onRequest } = require("firebase-functions/v2/https");
const admin = require("firebase-admin");
const cors = require("cors")({ origin: true }); // Permite requisições de qualquer origem (frontend)

// Inicializa o SDK do Firebase Admin para acessar o Firestore
admin.initializeApp();
const db = admin.firestore();

// Substitua pelo seu token real da AbacatePay (idealmente use Environment Variables)
const ABACATE_API_KEY = "Sua_Chave_Secreta_Da_AbacatePay_Aqui";

exports.criarCobranca = onRequest((req, res) => {
    // Executa o middleware do CORS para liberar o acesso ao Frontend
    cors(req, res, async () => {
        // Garante que a função só aceite requisições do tipo POST
        if (req.method !== "POST") {
            return res.status(405).json({ error: "Método não permitido. Use POST." });
        }

        try {
            const { produtoId, nome, email, cpf } = req.body;

            // 1. Busca o produto direto no Firestore para validar o preço real
            const produtoDoc = await db.collection("produtos").doc(produtoId).get();
            if (!produtoDoc.exists) {
                return res.status(404).json({ error: "Produto não encontrado" });
            }
            const produto = produtoDoc.data();

            // 2. Registra a intenção de compra na coleção "pedidos" com status PENDENTE
            const novoPedidoRef = db.collection("pedidos").doc();
            await novoPedidoRef.set({
                cliente: { nome, email, cpf },
                produtoId: produtoId,
                valor: produto.preco,
                status: "PENDENTE",
                criadoEm: admin.firestore.FieldValue.serverTimestamp()
            });

            // 3. Monta e envia o POST oficial para a AbacatePay
            const respostaAbacate = await fetch("https://abacatepay.com", {
                method: "POST",
                headers: {
                    "Authorization": `Bearer ${ABACATE_API_KEY}`,
                    "Content-Type": "application/json"
                },
                body: JSON.stringify({
                    frequency: "ONE_TIME",
                    methods: ["PIX"],
                    products: [
                        {
                            externalId: produtoId,
                            name: produto.nome,
                            quantity: 1,
                            priceValue: Math.round(produto.preco * 100) // Converte para centavos
                        }
                    ],
                    returnUrl: "https://sualoja.com", 
                    completionUrl: "https://sualoja.com",
                    customerId: cpf,
                    customer: {
                        name: nome,
                        email: email,
                        taxId: cpf
                    }
                })
            });

            const dadosAbacate = await respostaAbacate.json();

            if (!respostaAbacate.ok) {
                throw new Error(dadosAbacate.message || "Erro na API da AbacatePay");
            }

            // 4. Salva o ID da cobrança da AbacatePay no pedido do Firestore
            await novoPedidoRef.update({
                abacatePayId: dadosAbacate.data.id
            });

            // 5. Retorna a URL de pagamento para o frontend
            return res.json({ urlPagamento: dadosAbacate.data.url });

        } catch (error) {
            console.error("Erro interno na Cloud Function:", error);
            return res.status(500).json({ error: "Falha interna ao processar a cobrança." });
        }
    });
});
Use o código com cuidado.4. Como Fazer o Deploy (Publicação)Para enviar o código do seu computador para os servidores do Google Firebase, execute o seguinte comando no terminal (na raiz do projeto, fora da pasta functions):bashfirebase deploy --only functions
Use o código com cuidado.Após o término do deploy, o terminal exibirá a URL pública gerada para a sua função. Ela seguirá este padrão:https://criarcobranca-<id-do-seu-projeto>.<regiao>.cloudfunctions.net/criarCobranca5. Atualizando o seu frontend.jsAgora, basta abrir o arquivo frontend.js que criamos no passo anterior e substituir a URL antiga do Localhost pela nova URL fornecida pelo Firebase:javascript// Substitua o 'http://localhost:3000/api/criar-cobranca' pela URL do Firebase:
const resposta = await fetch('https://run.app', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(dadosCompra)
});
Use o código com cuidado.Se quiser finalizar a estrutura completa do sistema, posso te ajudar agora a:Criar a segunda Cloud Function para receber o Webhook da AbacatePay (o aviso automático de que o cliente pagou o PIX) para atualizar o Firestore para CONCLUIDO.Configurar o Firebase Emulators para testar as funções localmente sem precisar gastar internet ou fazer deploy a cada alteração.Proteger a chave secreta da AbacatePay usando o Secret Manager do Firebase em vez de deixá-la exposta no código.Como prefere continuar?
*************************************************************************************************************